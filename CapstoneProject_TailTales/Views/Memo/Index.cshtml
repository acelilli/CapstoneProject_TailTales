@model IEnumerable<CapstoneProject_TailTales.Models.Memo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Utenti.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataMemo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descrizione)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Completato)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Utenti.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DataMemo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descrizione)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Completato)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdMemo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdMemo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdMemo })
        </td>
    </tr>
}

</table>
