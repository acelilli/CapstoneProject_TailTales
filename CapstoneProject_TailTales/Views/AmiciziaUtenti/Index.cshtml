@model IEnumerable<CapstoneProject_TailTales.Models.AmiciziaUtenti>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Utenti.IdUtente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Utenti1.IdUtente)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Utenti.IdUtente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Utenti1.IdUtente)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdAmicizia }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdAmicizia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdAmicizia })
        </td>
    </tr>
}

</table>
